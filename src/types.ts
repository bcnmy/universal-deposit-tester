/** Status of an individual async operation */
export type Status = "idle" | "loading" | "success" | "error";

/** Visual status of a step in the pipeline UI */
export type StepStatus = "completed" | "active" | "pending" | "error";


